# This example was taken from the official documentation
target Python

main reactor timer_lag {
  timer t(0, 1 s)

  reaction(t) {=
    t = lf.time.logical_elapsed()
    T = lf.time.physical_elapsed()
    print(
        f"Elapsed logical time: {t}, physical time: {T}, lag: {T-t}"
    )
  =}
}
